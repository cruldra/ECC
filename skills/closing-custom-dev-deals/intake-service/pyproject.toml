[project]
name = "client-intake"
version = "0.1.0"
description = "业务信息收集表：客户在线填写，提交后落库"
authors = [{ name = "cruldra", email = "cruldra@gmail.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "jinja2>=3.1.6",
    "python-multipart>=0.0.9",
    "loguru>=0.7.3",
    "sqlalchemy>=2.0.0",
    "aiofiles>=23.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "config",
    "models",
    "routes",
    "static",
    "templates",
    "storage",
    "utils"
]

[tool.hatch.build.targets.wheel.force-include]
"main.py" = "main.py"
"config.py" = "config.py"
"__init__.py" = "__init__.py"
