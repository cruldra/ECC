---
description: Write a design spec after grilling has settled the idea. Does not write a PRD or an implementation plan.
argument-hint: "[feature idea | blank = use grilling consensus]"
---

# Spec Command

Thin entry over the `spec` skill. Follow that skill.

**Input**: `$ARGUMENTS`

If arguments are present, treat them as the topic. If this session already has confirmed grilling consensus, use that and do not re-interview.

If the idea is still open, stop and follow `grilling`.
